import { FormEvent, useState } from "react";
import { Link, useNavigate, useSearchParams } from "react-router-dom";
import { ArrowRight, CheckCircle2, Eye, EyeOff, UserCheck } from "lucide-react";
import AuthShell from "@/components/AuthShell";
import { api } from "@/lib/api";
export default function AcceptInvitation(){const[sp]=useSearchParams();const nav=useNavigate();const[p,setP]=useState("");const[p2,setP2]=useState("");const[show,setShow]=useState(false);const[show2,setShow2]=useState(false);const[msg,setMsg]=useState("");const[err,setErr]=useState("");const[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setErr("");setMsg("");if(p.length<10)return setErr("Password must be at least 10 characters.");if(p!==p2)return setErr("Passwords do not match.");setBusy(true);try{await api("/users/accept-invitation",{method:"POST",body:JSON.stringify({token:sp.get("token"),password:p})});setMsg("Account activated. Redirecting you to sign in…");setTimeout(()=>nav("/login"),1000);}catch(e:any){setErr(e.message||"Unable to activate account.");}finally{setBusy(false)}}return <AuthShell title="You’re invited to SecureFile." subtitle="Finish setting your password to activate your workspace account."><div className="auth-card-v2"><div className="auth-card-top"><span className="auth-icon-badge"><UserCheck size={20}/></span><div><p className="auth-eyebrow">Workspace invitation</p><h2>Set your password</h2><p>Choose a password you’ll use to sign in.</p></div></div>{err&&<div className="auth-error">{err}</div>}{msg&&<div className="auth-success"><CheckCircle2 size={16}/>{msg}</div>}<form onSubmit={submit}><label className="auth-field"><span>Password</span><div className="auth-password-wrap"><input type={show?"text":"password"} value={p} onChange={e=>setP(e.target.value)} placeholder="Create a strong password" required/><button type="button" onClick={()=>setShow(v=>!v)}>{show?<EyeOff size={17}/>:<Eye size={17}/>}</button></div></label><label className="auth-field"><span>Confirm password</span><div className="auth-password-wrap"><input type={show2?"text":"password"} value={p2} onChange={e=>setP2(e.target.value)} placeholder="Repeat your password" required/><button type="button" onClick={()=>setShow2(v=>!v)}>{show2?<EyeOff size={17}/>:<Eye size={17}/>}</button></div></label><button className="auth-submit" disabled={busy} aria-busy={busy}>{busy?"Activating…":"Activate account"}<ArrowRight size={17}/></button></form><Link className="auth-back-link" to="/login">Back to login</Link></div></AuthShell>}
