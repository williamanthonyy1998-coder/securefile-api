import{FormEvent,useState}from'react';import{useSearchParams,useNavigate}from'react-router-dom';import{api}from'../lib/api';export default function AcceptInvitation(){const[sp]=useSearchParams();const nav=useNavigate();const[p,setP]=useState(''),[p2,setP2]=useState(''),[msg,setMsg]=useState('');async function submit(e:FormEvent){e.preventDefault();if(p!==p2)return setMsg('Passwords do not match');try{await api('/users/accept-invitation',{method:'POST',body:JSON.stringify({token:sp.get('token'),password:p})});setMsg('Invitation accepted.');setTimeout(()=>nav('/login'),700)}catch(e:any){setMsg(e.message)}}return <div className="auth"><form className="form-card" onSubmit={submit}><h1>Activate account</h1><input type="password" minLength={10} required value={p} onChange={e=>setP(e.target.value)} placeholder="Create password"/><input type="password" minLength={10} required value={p2} onChange={e=>setP2(e.target.value)} placeholder="Confirm password"/><button className="btn">Activate</button>{msg&&<p>{msg}</p>}</form></div>}
