import{FormEvent,useState}from'react';import{Link}from'react-router-dom';import{api}from'../lib/api';export default function ForgotPassword(){const[email,setEmail]=useState('');const[msg,setMsg]=useState('');async function submit(e:FormEvent){e.preventDefault();const d=await api('/auth/forgot-password',{method:'POST',body:JSON.stringify({email})});setMsg(d.message)}return <div className="auth"><form className="form-card" onSubmit={submit}><h1>Forgot password</h1><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="Your verified email"/><button className="btn">Send reset email</button>{msg&&<p className="muted">{msg}</p>}<Link to="/login">Back to login</Link></form></div>}
