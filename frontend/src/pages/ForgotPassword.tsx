import { FormEvent, useState } from "react";
import { Link } from "react-router-dom";
import { ArrowLeft, Mail, Send } from "lucide-react";
import AuthShell from "@/components/AuthShell";
import { api } from "@/lib/api";
export default function ForgotPassword(){ const [email,setEmail]=useState(""); const [busy,setBusy]=useState(false); async function submit(e:FormEvent){e.preventDefault();setBusy(true);try{const d=await api("/auth/forgot-password",{method:"POST",body:JSON.stringify({email})});window.dispatchEvent(new CustomEvent("sf:alert",{detail:{type:"success",message:d.message||"If the email exists, a reset message has been sent."}}));}catch(e:any){window.dispatchEvent(new CustomEvent("sf:alert",{detail:{type:"error",message:e.message||"Unable to send reset email."}}));}finally{setBusy(false)}} return <AuthShell title="Get back in, securely." subtitle="We’ll help you reset your password without exposing account information."><div className="auth-card-v2"><div className="auth-card-top"><span className="auth-icon-badge"><Mail size={20}/></span><div><p className="auth-eyebrow">Account recovery</p><h2>Forgot password?</h2><p>Enter your verified account email.</p></div></div><form onSubmit={submit}><label className="auth-field"><span>Email address</span><input autoFocus type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@company.com" required/></label><button className="auth-submit" disabled={busy} aria-busy={busy}>{busy?"Sending…":"Send reset email"}<Send size={17}/></button></form><Link className="auth-back-link" to="/login"><ArrowLeft size={15}/> Back to login</Link></div></AuthShell> }
