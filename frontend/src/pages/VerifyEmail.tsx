import {useEffect,useState} from 'react';import {useSearchParams,useNavigate} from 'react-router-dom';import {api} from '../lib/api';
export default function VerifyEmail(){const[sp]=useSearchParams();const[message,setMessage]=useState('Verifying...');const nav=useNavigate();useEffect(()=>{const token=sp.get('token');if(!token){setMessage('Verification link is missing.');return;}api('/auth/verify-email',{method:'POST',body:JSON.stringify({token})}).then(()=>setMessage('Email verified successfully. You can now log in.')).catch(e=>setMessage(e.message));},[]);return <div className="auth"><div className="form-card"><h1>Email verification</h1><p>{message}</p><button className="btn" onClick={()=>nav('/login')}>Go to Login</button></div></div>}
