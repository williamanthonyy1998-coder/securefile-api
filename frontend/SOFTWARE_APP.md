# SecureFile Software Frontend

This folder is the authenticated SecureFile software/workspace application.

The public marketing website is now a separate top-level application in `../website`.

- `frontend/` = SecureFile software
- `backend/` = SecureFile API
- `website/` = public website / pricing

The software frontend still owns onboarding/auth pages such as `/signup`, `/login`, verification, password reset and invitation acceptance.
