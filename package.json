{
  "name": "securefile-platform",
  "private": true,
  "scripts": {
    "dev": "npm --prefix backend run db:generate && npm --prefix backend run db:sync && npm --prefix backend run db:backfill-personal && concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\" \"npm --prefix website run dev\"",
    "build": "npm --prefix backend run db:generate && npm --prefix backend run build && npm --prefix frontend run build && npm --prefix website run build",
    "start": "npm --prefix backend start",
    "db:generate": "npm --prefix backend run db:generate",
    "db:sync": "npm --prefix backend run db:sync",
    "db:migrate:deploy": "npm --prefix backend run migrate:deploy",
    "db:backfill-personal": "npm --prefix backend run db:backfill-personal",
    "db:seed": "npm --prefix backend run db:seed",
    "db:reset": "npm --prefix backend run db:reset",
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install && npm --prefix website install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "version": "1.0.0"
}
