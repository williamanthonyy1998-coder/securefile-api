{
  "name": "securefile-sharing-software",
  "private": true,
  "scripts": {
    "dev": "npm run db:generate && concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "build": "npm run db:generate && npm --prefix backend run build && npm --prefix frontend run build",
    "start": "npm --prefix backend start",
    "db:generate": "prisma generate --schema prisma/schema.prisma",
    "db:migrate": "prisma migrate dev --schema prisma/schema.prisma",
    "db:seed": "tsx prisma/seed.ts",
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "db:where": "node -e \"console.log(require.resolve(\"@prisma/client\"))\"",
    "db:push": "prisma db push --schema prisma/schema.prisma",
    "db:validate": "prisma validate --schema prisma/schema.prisma"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "prisma": "^6.13.0",
    "tsx": "^4.20.3"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "bcryptjs": "^3.0.2"
  }
}
