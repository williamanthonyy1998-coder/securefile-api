# User limit fix

Super Admin company creation/edit now has an explicit Users Included field. The value is stored in the company subscription as the purchased user limit. It no longer silently defaults the purchased limit to one. The actual Company Admin account is still one account; the user limit controls how many company users can be created.
